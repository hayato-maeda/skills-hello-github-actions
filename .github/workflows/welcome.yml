name: cloc

on:
  pull_request:
    types:
      - opened
      - synchronize

permissions:
  pull-requests: write

jobs:
  cloc:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Install cloc
        run: |
         wget https://github.com/AlDanial/cloc/releases/download/2.00/cloc-2.00.pl
          chmod +x cloc-2.00.pl
          sudo mv cloc-2.00.pl /usr/local/bin/cloc

      - name: Run cloc
        id: run-cloc
        run: |
          git fetch origin main
          cloc --version > cloc_output.txt

      - name: cloc results
        uses: actions/github-script@v6
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          script: |
            const fs = require('fs');
            const txt = fs.readFileSync('cloc_output.txt', 'utf8');
            const output = `\`\`\`\n${txt}\n\`\`\``;
            const comments = await github.rest.issues.listComments({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo
            });
            const botComment = comments.data.find(comment => comment.user.login === 'github-actions[bot]');
            if (botComment) {
              await github.rest.issues.updateComment({
                comment_id: botComment.id,
                owner: context.repo.owner,
                repo: context.repo.repo,
                body: output
              });
            } else {
              await github.rest.issues.createComment({
                issue_number: context.issue.number,
                owner: context.repo.owner,
                repo: context.repo.repo,
                body: output
              });
            }

      - name: Cleanup
        run: rm cloc_output.txt
